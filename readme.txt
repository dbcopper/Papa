你是一个资深桌面应用工程师 + 前端动画工程师。请为我生成一个“常驻轻量桌面浮窗电子宠物”的可运行最小项目（MVP），并给出关键代码与文件结构。要求以可落地为第一目标。【目标产品】- 一个桌面透明浮窗的小动物（2D 分层 PNG + 代码动画），常驻桌面 always-on-top，不抢焦点，低打扰。- 支持把任意文件拖到宠物上（dragover/drop）。拖入时宠物进入“准备吃”状态；放下后播放“吃一口”动画。- 放下文件后弹出一个小气泡面板，展示 3 个常用操作按钮：  1) Summarize（总结）  2) Extract action items（提取待办）  3) Remember（记住：保存到本地数据库）- 先不接真实 LLM：用 mock（假数据）实现按钮点击后的流式输出（逐字打印效果），后续我再接 API。- 数据本地存储：SQLite（存文件路径、hash、时间、摘要/待办/记忆内容、tags）。- 必须极度轻量：idle 状态不做任何网络请求、不做后台扫描；动画低频；不使用高帧率循环。【技术栈要求】- 桌面容器：Tauri v2（Rust 后端 + WebView 前端），跨平台（至少 macOS/Windows 可运行）。- 前端：React + TypeScript（或原生 TS 也可，但要清晰），样式可以用最简单 CSS。- 动画：不要引入重型动画引擎。优先 Web Animations API 或 CSS 动画。- 后端：Rust 负责窗口配置（透明、置顶）、拖拽事件、文件读取/计算 hash、SQLite 读写。- 前后端通信：Tauri commands + events。【窗口/交互细节】- 窗口：透明背景、无边框、always-on-top，默认右下角，支持拖拽移动。- 交互：宠物区域可点击唤出输入框（MVP 可只做气泡面板）。- dragover：显示高亮描边或宠物抬头状态；drop：触发“吃一口”动画 + 显示按钮面板。- 气泡面板自动消失：10 秒无交互或点击空白处关闭。- 需要一个“静音/休眠”开关（MVP 简单实现：右键菜单或一个小按钮）。【宠物状态机】实现一个简单状态机，并在代码中清晰定义：- idle_breathe（默认）- idle_blink（随机触发）- drag_over_focus- eat_chomp- thinking (mock 流式输出时)- success_happy / error_confused（按钮执行后）【输出内容要求】1) 给出项目目录结构（前端/后端）与每个文件职责说明。2) 给出关键配置：Tauri 窗口透明/置顶/无边框/初始位置。3) 给出 Rust 端核心代码：   - 接收 drop 的文件路径（或拖拽 payload）   - 计算文件 hash（至少支持文本、pdf 先不解析内容也行）   - 写入 SQLite（schema 你设计）   - 通过 event 通知前端更新 UI4) 给出前端核心代码：   - 宠物渲染（分层 PNG：body/eyes_open/eyes_closed/mouth/tail）   - 状态机驱动动画（呼吸、眨眼、吃一口）   - dragover/drop 交互   - 气泡面板 + 3 个按钮   - mock 流式输出组件（逐字打印）5) 给出运行步骤（pnpm/npm + tauri dev），以及常见坑（跨平台拖拽/透明窗）提示。6) 代码要尽量完整可运行，不要只给伪代码；如果某平台特性做不到，请说明替代方案。【约束】- 不要使用任何云端依赖（除 mock）。- 不要在 idle 时轮询或 setInterval 高频刷新。- 所有动画应可配置频率（例如眨眼随机 20~45 秒）。- 给出至少 1 个简单的 PNG 占位方案（例如用 CSS 圆形代替也行），保证即使没有素材也能跑起来。现在开始输出：先给项目结构和关键配置，再给代码块。所有代码请按文件名分段输出。